@model IEnumerable<System_Rezerwacji.Models.Reservation>
@{
    ViewData["Title"] = "Panel zarządzania";
    ViewData["PageTitle"] = "Zarządzanie rezerwacjami";
}

<div class="card">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-3">
            <a asp-action="Create" class="btn btn-primary">Nowa rezerwacja</a>
        </div>

        @if (!Model.Any())
        {
            <div class="empty-state">Brak rezerwacji do wyświetlenia.</div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead>
                        <tr>
                            <th>
                                <a asp-action="Manage" asp-route-sortOrder="@ViewBag.DateSortParm">Data</a>
                            </th>
                            <th>
                                <a asp-action="Manage" asp-route-sortOrder="@ViewBag.CustomerSort">Klient</a>
                            </th>
                            <th>
                                <a asp-action="Manage" asp-route-sortOrder="@ViewBag.ServiceNameSort">Usługa</a>
                            </th>
                            <th class="text-end">Akcje</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.ReservationDate.ToString("yyyy-MM-dd HH:mm")</td>
                                <td>@item.Customer.FullName</td>
                                <td>@item.Service.Name</td>
                                <td class="text-end">
                                    <div class="btn-group btn-group-sm">
                                        <a asp-action="Details" asp-route-id="@item.ReservationID" class="btn btn-outline-primary">Szczegóły</a>
                                        <a asp-action="Edit" asp-route-id="@item.ReservationID" class="btn btn-outline-secondary">Edytuj</a>
                                        <a asp-action="Delete" asp-route-id="@item.ReservationID" class="btn btn-outline-danger">Usuń</a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
